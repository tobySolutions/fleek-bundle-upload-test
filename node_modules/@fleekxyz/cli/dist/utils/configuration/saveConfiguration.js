"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveConfiguration = void 0;
const fs_1 = require("fs");
const saveConfiguration = async ({ config, format }) => {
    const prettyStringifiedConfig = JSON.stringify(config, undefined, 2);
    if (format === 'ts') {
        const content = `import { FleekConfig } from '@fleekxyz/cli';\n\nexport default ${prettyStringifiedConfig} satisfies FleekConfig;`;
        await fs_1.promises.writeFile('fleek.config.ts', content);
    }
    if (format === 'js') {
        const content = `/** @type {import('@fleekxyz/cli').FleekConfig} */\nmodule.exports = ${prettyStringifiedConfig};`;
        await fs_1.promises.writeFile('fleek.config.js', content);
    }
    if (format === 'json') {
        await fs_1.promises.writeFile('fleek.config.json', prettyStringifiedConfig);
    }
};
exports.saveConfiguration = saveConfiguration;
//# sourceMappingURL=saveConfiguration.js.map